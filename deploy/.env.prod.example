# Production environment (Docker Hub)
# Copy to .env.prod and update values

# Docker Hub username (required)
DOCKER_USERNAME=your-docker-hub-username

# Images use :prod tag (auto-updated on each master push)
# See docker-compose.prod.yml for full image names
