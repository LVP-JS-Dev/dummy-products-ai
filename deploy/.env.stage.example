# Stage environment (Docker Hub)
# Copy to .env.stage and update values

# Docker Hub username (required)
DOCKER_USERNAME=your-docker-hub-username

# Images use :stage tag (auto-updated on each develop push)
# See docker-compose.stage.yml for full image names
