name: CI/CD

on:
  push:
    branches:
      - master
      - develop
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'stage'
        type: choice
        options:
          - stage
          - prod

# Secrets (required): DOCKER_USERNAME, DOCKER_TOKEN, NPM_TOKEN
# Variables (optional fallback): DOCKER_USERNAME
# See .github/.env.example for setup instructions

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME || vars.DOCKER_USERNAME }}

jobs:
  build_web:
    name: Build and push web image
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate credentials
        run: |
          if [ -z "${{ secrets.DOCKER_USERNAME }}" ] && [ -z "${{ vars.DOCKER_USERNAME }}" ]; then
            echo "::error::DOCKER_USERNAME not set. Add as secret or variable in repository settings."
            exit 1
          fi
          if [ -z "${{ secrets.DOCKER_TOKEN }}" ]; then
            echo "::error::DOCKER_TOKEN not set. Add as secret in repository settings."
            exit 1
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME || vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Generate image tag
        id: meta
        run: |
          set -euo pipefail
          GIT_SHA="$(echo "${GITHUB_SHA}" | cut -c1-7)"
          DOCKER_USER="${{ secrets.DOCKER_USERNAME || vars.DOCKER_USERNAME }}"
          
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            # Production release
            VERSION="${GITHUB_REF#refs/tags/}"
            echo "tag=${DOCKER_USER}/dummy-products-web:${VERSION}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-web:latest" >> $GITHUB_OUTPUT
          elif [[ "${GITHUB_REF}" == "refs/heads/master" ]]; then
            echo "tag=${DOCKER_USER}/dummy-products-web:prod-${GIT_SHA}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-web:prod" >> $GITHUB_OUTPUT
          else
            # develop or other branches
            echo "tag=${DOCKER_USER}/dummy-products-web:stage-${GIT_SHA}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-web:stage" >> $GITHUB_OUTPUT
          fi

      - name: Build and push web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/web.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.meta.outputs.tag }}
            ${{ steps.meta.outputs.tag_latest }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build_docs:
    name: Build and push docs image
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate credentials
        run: |
          if [ -z "${{ secrets.DOCKER_USERNAME }}" ] && [ -z "${{ vars.DOCKER_USERNAME }}" ]; then
            echo "::error::DOCKER_USERNAME not set. Add as secret or variable in repository settings."
            exit 1
          fi
          if [ -z "${{ secrets.DOCKER_TOKEN }}" ]; then
            echo "::error::DOCKER_TOKEN not set. Add as secret in repository settings."
            exit 1
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME || vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Generate image tag
        id: meta
        run: |
          set -euo pipefail
          GIT_SHA="$(echo "${GITHUB_SHA}" | cut -c1-7)"
          DOCKER_USER="${{ secrets.DOCKER_USERNAME || vars.DOCKER_USERNAME }}"
          
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
            echo "tag=${DOCKER_USER}/dummy-products-docs:${VERSION}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-docs:latest" >> $GITHUB_OUTPUT
          elif [[ "${GITHUB_REF}" == "refs/heads/master" ]]; then
            echo "tag=${DOCKER_USER}/dummy-products-docs:prod-${GIT_SHA}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-docs:prod" >> $GITHUB_OUTPUT
          else
            echo "tag=${DOCKER_USER}/dummy-products-docs:stage-${GIT_SHA}" >> $GITHUB_OUTPUT
            echo "tag_latest=${DOCKER_USER}/dummy-products-docs:stage" >> $GITHUB_OUTPUT
          fi

      - name: Build and push docs image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/docs.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.meta.outputs.tag }}
            ${{ steps.meta.outputs.tag_latest }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish_burlaki:
    name: Publish burlaki
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.11"

      - name: Verify tag matches package version
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="$(node -p "require('./packages/burlaki/package.json').version")"
          if [ "v${VERSION}" != "$TAG" ]; then
            echo "Tag ($TAG) does not match package.json version (v${VERSION})."
            exit 1
          fi

      - name: Publish package
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${NPM_TOKEN}" ]; then
            echo "::error::NPM_TOKEN not set. Add as secret in repository settings."
            exit 1
          fi
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
          corepack enable
          pnpm install --frozen-lockfile
          pnpm -C packages/burlaki publish --access public
