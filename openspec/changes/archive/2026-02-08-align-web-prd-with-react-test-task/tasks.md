# Tasks: Align Web PRD With React Test Task

## 1. PRD Alignment

- [x] 1.1 Rewrite `/apps/web/PRD.md` to include assignment-aligned functional requirements (`FR-1..FR-7`) and updated DoD.
- [x] 1.2 Remove conflicting statements in `/apps/web/PRD.md` about absent auth/API and replace with external API integration requirements.
- [x] 1.3 Add a traceability table in `/apps/web/PRD.md` mapping assignment requirements to PRD sections and DoD.

## 2. Cross-PRD Consistency

- [x] 2.1 Update `/PRD.md` out-of-scope wording to allow external auth/products API usage in `apps/web` while keeping no-custom-backend constraints.
- [x] 2.2 Verify `packages/ui-kit` and `apps/fumadocs` PRDs remain consistent with the new `apps/web` scope; add only minimal clarifications if needed.

## 3. Validation and Review

- [x] 3.1 Run a final terminology pass across all PRDs (`source of truth`, `scope`, `DoD`) to remove contradictions.
- [x] 3.2 Validate OpenSpec artifacts and capture any unresolved questions (e.g., editing scope ambiguity) before implementation starts.
